# Clippy's own configuration, which is separate from the lint levels: those are
# in Cargo.toml's [workspace.lints] for cargo, and on the command line
# crates/bazel-tools' clippy runs for Bazel.
#
# Nothing is set. The file exists because clippy under Bazel is handed a
# configuration file rather than left to search for one.
