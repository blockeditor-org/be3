# The build script asks pkg-config how to link libasound. It is answered from
# the sysroot in buck/sysroot, and the -lasound it prints reaches the link.
[buildscript.run]
rustc_link_lib = true
env = { PKG_CONFIG = "$(exe //buck/sysroot:pkg-config)" }
