# The build script asks pkg-config how to link libseat. It is answered from the
# sysroot in buck/sysroot, and the -lseat it prints reaches the link.
[buildscript.run]
rustc_link_lib = true
env = { PKG_CONFIG = "$(exe_target //buck/sysroot:pkg-config)" }
