# The APK carries every plugin as a precompiled module, and compressing those
# assets runs out of Gradle's default 512 MB heap.
org.gradle.jvmargs=-Xmx4g -XX:MaxMetaspaceSize=1g -Dfile.encoding=UTF-8
