load("@root//buck/cargo:defs.bzl", "cargo_binary", "cargo_library", "cargo_test")

# The build's own tools: generate (crates.bzl, which buck/cargo/buckify.bxl
# builds with cargo, since it runs before the rules exist), clippy and
# rust-project (for //:verify and //:rust-project), resolve (the sysroot's
# lockfile) and apk.

cargo_library()

cargo_test()

cargo_binary()
