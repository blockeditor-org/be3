[package]
name = "reactive"
description = "Single-threaded reactive signals, memos, effects, and scopes"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
reactive_macros = { path = "../reactive_macros" }

[lints]
workspace = true
