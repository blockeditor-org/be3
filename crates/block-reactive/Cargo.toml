[package]
name = "block-reactive"
description = "Reactive projections of block-client blocks"
edition.workspace = true
version.workspace = true
license.workspace = true

[dependencies]
block = { path = "../block" }
block-client = { path = "../block-client" }
reactive = { path = "../reactive" }
uuid = "1"

[dev-dependencies]
serde = { version = "1", features = ["derive"] }

[lints]
workspace = true
